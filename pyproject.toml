[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "monitoring-hub-forge"
version = "0.0.0"
description = "Kind-extensible engine that turns monitoring manifests into distributable artifacts"
requires-python = ">=3.12"
license = "Apache-2.0"
dependencies = [
    "click>=8.1",
    "httpx>=0.28.1",
    "jinja2>=3.1.5",
    "pydantic>=2.9",
    "pyyaml>=6",
    "tenacity>=9.1.4",
]

[project.scripts]
mh = "forge.cli.main:cli"

[dependency-groups]
dev = [
    "ruff>=0.8",
    "mypy>=1.13",
    "pytest>=8.3",
    "pytest-xdist>=3.6",
    "pytest-cov>=6.0",
    "types-PyYAML>=6",
    "bandit>=1.8",
    "pip-audit>=2.7",
    "pre-commit>=4.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/forge"]

[tool.bandit]
exclude_dirs = ["tests"]

[tool.ruff]
target-version = "py312"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "PL"]

[tool.ruff.lint.per-file-ignores]
# Test assertions routinely compare against literal values (modes, exit codes,
# digests); PLR2004 "magic value" is noise in that context.
"tests/**" = ["PLR2004"]

[tool.mypy]
plugins = ["pydantic.mypy"]
python_version = "3.12"
strict = true
files = ["src", "tests", "scripts"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --strict-markers --cov=forge --cov-report=term-missing"
